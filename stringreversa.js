const frase = "Espero que gostem, garanto total comprometimento e dedicação!"; // Altere a string para testar!
let inverte = "";

for (let i = frase.length - 1; i >= 0; i--) {
  inverte += frase[i];
}

console.log(inverte); 
